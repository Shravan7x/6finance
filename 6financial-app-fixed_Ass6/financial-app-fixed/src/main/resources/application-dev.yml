spring:
  datasource:
    url: jdbc:h2:mem:devdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: DEBUG
    com.example.financialapp: DEBUG

external:
  service:
    payment-url: https://dev-payment.financial.com/api
    audit-url: https://dev-audit.financial.com/api
