spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: WARN
    com.example.financialapp: INFO

external:
  service:
    payment-url: https://test-payment.financial.com/api
    audit-url: https://test-audit.financial.com/api
